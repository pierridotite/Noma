// Basic arithmetic and operators test

fn test_operators() {
    let a = 10;
    let b = 20;
    
    let sum = a + b;
    let diff = a - b;
    let prod = a * b;
    let quot = b / a;
    
    let is_equal = a == b;
    let is_less = a < b;
    let is_greater = a > b;
}

fn test_keywords() {
    learn x = 1.0;
    let mut y = 2.0;
    
    tensor<3x3> matrix;
    
    return x + y;
}
